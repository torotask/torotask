# Shared Redis config for local apps (tester, dashboard, etc.)
# Copy to .env and adjust as needed:
#   cp .env.example .env

# Use a non-default Redis database (default is 0)
REDIS_DB=1

# Or set the full connection URL instead:
# REDIS_URL=redis://localhost:6379/1
