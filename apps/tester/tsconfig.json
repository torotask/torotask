{
  "extends": "../../tsconfig.base.json", // Assume this path will be correct eventually or adjusted by user
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "tsBuildInfoFile": ".tsbuildinfo"
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts"],
  "exclude": ["node_modules", "dist"],
  "references": [{ "path": "../../packages/torotask" }]
}
